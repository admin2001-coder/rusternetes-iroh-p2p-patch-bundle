--- Cargo.toml.before-iroh-overlay	2026-04-29 03:15:02.147585052 +0000
+++ Cargo.toml	2026-04-29 03:15:04.810876694 +0000
@@ -6,6 +6,7 @@
     "crates/controller-manager",
     "crates/kubelet",
     "crates/kube-proxy",
+    "crates/iroh-overlay",
     "crates/kubectl",
     "crates/common",
     "crates/storage",
--- crates/kube-proxy/Cargo.toml.before-iroh-overlay	2026-04-29 03:15:02.164194116 +0000
+++ crates/kube-proxy/Cargo.toml	2026-04-29 03:15:04.811614280 +0000
@@ -16,10 +16,12 @@
 [features]
 default = []
 sqlite = ["rusternetes-storage/sqlite"]
+iroh-overlay = ["dep:rusternetes-iroh-overlay"]
 
 [dependencies]
 rusternetes-common = { path = "../common" }
 rusternetes-storage = { path = "../storage" }
+rusternetes-iroh-overlay = { path = "../iroh-overlay", optional = true }
 tokio.workspace = true
 anyhow.workspace = true
 tracing.workspace = true
--- crates/kube-proxy/src/lib.rs.before-iroh-overlay	2026-04-29 03:15:02.181594718 +0000
+++ crates/kube-proxy/src/lib.rs	2026-04-29 03:15:04.832821874 +0000
@@ -25,6 +25,29 @@
         warn!("Kube-proxy requires iptables to be installed and accessible.");
     }
 
+    #[cfg(feature = "iroh-overlay")]
+    let overlay_runtime = if let Some(overlay_config) =
+        rusternetes_iroh_overlay::IrohOverlayConfig::from_env(config.node_name.clone())?
+    {
+        Some(
+            rusternetes_iroh_overlay::IrohOverlayRuntime::start(
+                Arc::clone(&storage),
+                overlay_config,
+            )
+            .await?,
+        )
+    } else {
+        None
+    };
+
+    #[cfg(feature = "iroh-overlay")]
+    let kube_proxy = Arc::new(tokio::sync::Mutex::new(KubeProxy::new_with_overlay(
+        Arc::clone(&storage),
+        config.node_name.clone(),
+        overlay_runtime,
+    )?));
+
+    #[cfg(not(feature = "iroh-overlay"))]
     let kube_proxy = Arc::new(tokio::sync::Mutex::new(KubeProxy::new(Arc::clone(&storage))?));
 
     info!("Kube-proxy initialized successfully");
--- crates/kube-proxy/src/proxy.rs.before-iroh-overlay	2026-04-29 03:15:02.198457633 +0000
+++ crates/kube-proxy/src/proxy.rs	2026-04-29 03:15:04.834229550 +0000
@@ -3,10 +3,13 @@
 use rusternetes_storage::{StorageBackend, Storage};
 use std::collections::HashMap;
 use std::sync::Arc;
-use tracing::{debug, error, info};
+use tracing::{debug, error, info, warn};
 
 use crate::iptables::IptablesManager;
 
+#[cfg(feature = "iroh-overlay")]
+use rusternetes_iroh_overlay::IrohOverlayRuntime;
+
 /// KubeProxy manages service networking through iptables rules
 pub struct KubeProxy {
     storage: Arc<StorageBackend>,
@@ -15,10 +18,38 @@
     /// K8s uses iptables-restore for atomic updates; we approximate by skipping
     /// sync when state hasn't changed, eliminating the flush gap.
     last_sync_hash: u64,
+    #[cfg(feature = "iroh-overlay")]
+    node_name: String,
+    #[cfg(feature = "iroh-overlay")]
+    overlay: Option<Arc<IrohOverlayRuntime>>,
 }
 
 impl KubeProxy {
     pub fn new(storage: Arc<StorageBackend>) -> Result<Self> {
+        #[cfg(feature = "iroh-overlay")]
+        {
+            return Self::new_with_overlay(storage, String::new(), None);
+        }
+
+        #[cfg(not(feature = "iroh-overlay"))]
+        {
+            let iptables = IptablesManager::new();
+            iptables.initialize()?;
+
+            Ok(Self {
+                storage,
+                iptables,
+                last_sync_hash: 0,
+